<?xml version="1.0"?>

<!DOCTYPE project SYSTEM "../dtd/Project.dtd"> 

<project default="testBasic">
    
    <target name="testBasic">

        <webtest name="LoadMainPage">
            <invoke
                url="${wgServer}${wgScript}/Main_Page"
                description="Tests if wiki responds at all by loading the main page" 
            />
            <verifyTitle text="${wgSitename}" />
            <verifyXPath
                description="Check if edit link contains mode WYSIWYG"
                xpath="//div[@id='ca-edit']/a/@href"
                text="${wgScript}?title=Main_Page&amp;action=edit&amp;mode=wysiwyg" />
        </webtest>

    </target>
</project>
