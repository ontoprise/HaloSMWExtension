C({"name": "steal.static.plugins", "type": "function", "parent": "steal.static", "comment": "<p>Loads a list of plugins given a path relative to steal's ROOT folder.</p>\n\n<p>Steal.plugins is used to load relative to ROOT no matter where the current file is \nlocated.  For example, if you want to load the 'foo/bar' plugin that is located like:</p>\n\n<pre><code class='javascript'>steal\\\nfoo\\\n   bar\\\n      bar.js</code></pre>\n\n<p>You can load it like:</p>\n\n<pre><code class='javascript'>steal.plugins('foo/bar');</code></pre>\n\n<p>It should be noted that plugins always looks for a JS file that shares the name of the\nplugin's folder (bar.js is in bar).</p>", "params": {"plugin_location": {"order": 0, "description": "<p>location of a plugin, ex: jquery/dom/history.</p>", "name": "plugin_location", "type": "String", "optional": false}}, "ret": {"description": "<p>a new steal object</p>", "type": "steal"}, "children": []})